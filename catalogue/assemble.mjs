import { readFileSync, writeFileSync } from 'node:fs';
import { CARDS } from './cards.mjs';
const at = (n) => readFileSync(new URL('./' + n, import.meta.url), 'utf8');

const VERSION = JSON.parse(readFileSync(new URL('../package.json', import.meta.url), 'utf8')).version;
// Stamped so a reader can tell whether the "Сейчас" column is still true.
const STAMP = new Date().toISOString().slice(0, 10).split('-').reverse().join('.');

const page = `${at('head.html')}
<div class="wrap">
  <div class="lede">
    <h1>Что придёт в Telegram</h1>
    <p>Каждый вид уведомления: где появляется, чем запускается и как выглядит его карточка сегодня. ${CARDS.length} видов.</p>
    <p class="meta">Карточки напечатал сам пакет уведомлений, теми же аргументами, какие передают настоящие отправители. Ничего не нарисовано руками, и сборка страницы падает, если строка тегов у карточки разошлась с тем, что даёт её отправитель — именно так шесть карточек однажды показывали тег, которого не бывает. Собрано ${STAMP}. Отрисовано исходниками пакета: выпущен ${VERSION}, сегодняшние правки в него ещё не вошли.</p>
  </div>

  <nav>${at('nav.html')}</nav>

  <section class="block" id="types">
    <h2>Какие теги бывают и что стоит во второй строке</h2>
    <p>Первая строка карточки — два тега. Первый говорит, ЧТО это за событие, и он всегда один из этих восьми. Второй — машинный ключ: по нему разборщик находит пару «сломалось → починилось», поэтому он английский и обязан быть ОДНИМ И ТЕМ ЖЕ у красной карточки и у зелёной, которая её закрывает. Исключений нет: строку тегов несёт даже дневной отчёт сервера, у которого пакет строит не тело, а только доставку — тег это твой фильтр, и к формату тела он отношения не имеет.</p>
    <p>Вторая строка — это <b>значок и слово</b>: <code>значок <b>Тип:</b> что случилось</code>. Значок принадлежит слову: внутри одного тега двух одинаковых значков не бывает — увидев 🙋 в ленте задач, ты уже знаешь, что кто-то взял задачу на себя, не читая строку. И наоборот: один смысл выглядит одинаково во всех тегах — сломалось всегда 🔴, будь то выкатка, проверка или задача.</p>
    <p><b>Звук решает значок, и только он.</b> Звонят четыре: 🔴 🚨 🚫 ❓. Остальные приходят молча — правило одно на весь пакет, без исключений. Ни одна карточка про PR не звонит: ревьюер, попросивший правки, это не поломка, и будить тебя ночью ему незачем. Раньше правил было два, значок у этой карточки был красный, а звука не было — и вместо того чтобы починить значок, чинили звук.</p>
    <div class="tablewrap"><table class="icons">
      <thead><tr><th>Значок</th><th>Что означает</th><th>Звук</th></tr></thead>
      <tbody>
${at('icons.html')}
      </tbody>
    </table></div>
    <p>Ниже — все варианты, какие пакет умеет напечатать для каждого тега. Они не написаны руками: при сборке страницы каждый прогоняется через сам рендерер, а сборка падает, если значок и звук у него разошлись или если значку не написано, что он означает.</p>
    <div class="tablewrap"><table>
      <thead><tr><th>Тег</th><th>Вторая строка карточки, звук и что это значит</th><th>Кто шлёт</th></tr></thead>
      <tbody>
${at('types.html')}
      </tbody>
    </table></div>
  </section>

  <section class="block" id="cuts">
    <h2>Почему текст местами обрезается</h2>
    <p>Четыре разных правила, и каждое режет по-своему. Если увидишь «…» или уголок ⌄ — это одно из них.</p>
    <ul class="rules">
      <li><b>Поле держит одну строку</b><span>Строка вида <code>Title: …</code> — это ОДНА строка по договору. Если значение пришло в несколько строк, остаётся первая и к ней дописывается «…». Так и потерялось имя сессии в старой карточке прогара: оно лежало полем. Теперь оно лежит цитатой и приходит целиком.</span></li>
      <li><b>Цитата длиннее 400 знаков сворачивается</b><span>Уголок ⌄ в конце цитаты — это Telegram, а не обрезка. Текст на месте, он просто сложен: нажми и он развернётся. Иначе стектрейс или дамп лога растянул бы карточку на весь экран.</span></li>
      <li><b>Вся карточка режется на 4000 знаках</b><span>У Telegram потолок 4096; строка тегов дописывается уже после реза, так что на текст остаётся чуть меньше. Пакет режет аккуратно: по границе строки — но только если так остаётся больше 60% содержимого, иначе ровно по счёту, — никогда посередине тега или символа вроде <code>&amp;nbsp;</code>, и дозакрывает всё, что открыл, в обратном порядке. Иначе Telegram отвечает ошибкой и карточка пропадает ЦЕЛИКОМ — так уже было, из-за одного длинного заголовка группы.</span></li>
      <li><b>Карточка с вложением — 1024 знака</b><span>У Telegram для сообщения с файлом свой, более низкий потолок. Любая карточка, к которой прицеплен файл, становится подписью к нему и режется по нему, а не по 4000. Сам файл приезжает целиком — режется только текст вокруг него.</span></li>
    </ul>
  </section>

${at('articles.html')}

  <footer>
    <p>Значок отвечает на вопрос «что именно случилось», а не «хорошо или плохо»: 🙋 задачу взяли, 🎉 влили, 📝 просят правки, 🗑️ закрыли ни с чем, 🚫 выключено, ❓ молчит. Полный список — в таблице значков выше.</p>
    <p>По-русски в карточке остаётся только написанное человеком: тело коммита, заголовок задачи, строка, которой ты открыл сессию. Всё, что пишет машина, — по-английски, и это проверяет сторож на каждом прогоне конфига.</p>
    <p>Со звуком приходят 🔴 🚨 🚫 ❓, остальные — молча. «Форум» — это группа в Telegram с вкладками: у каждого проекта своя, и внутри неё вкладка Ops, куда идут все карточки проекта.</p>
    <p>Колонка «Сейчас» — состояние на день сборки, указанный вверху. Она не обновляется сама.</p>
  </footer>
</div>`;

writeFileSync(new URL('./telegram-cards.html', import.meta.url), page);
console.log('page written,', page.length, 'chars');
